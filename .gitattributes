*.mp4 filter=lfs diff=lfs merge=lfs -text
# Ship hero as a normal blob so Vercel/GitHub deploys real bytes (LFS pointers break <video> on static hosts)
public/hero/hero-background.mp4 -filter -diff -merge
